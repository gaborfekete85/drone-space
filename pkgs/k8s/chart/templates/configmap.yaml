apiVersion: v1
kind: ConfigMap
metadata:
  name: drone-config
  labels:
    {{- include "drone.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
